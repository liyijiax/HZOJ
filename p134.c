#include <stdio.h>

int main() {
    int a, b, falg = 0;
    for (int i = (a - 1) / 11 + 1; i <= b; i += 11) {
        flag && printf(" ");
        pritnf("i");
        flag = 1;
    }
    return 0;
}